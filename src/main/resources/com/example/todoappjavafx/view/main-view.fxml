<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.todoappjavafx.controller.MainController">

    <!-- Header -->
    <top>
        <HBox spacing="10" padding="10" style="-fx-background-color: #2d2d2d;">
            <Label text="✅ TODO App" style="-fx-text-fill: white; -fx-font-size: 18;" />
            <Region HBox.hgrow="ALWAYS"/>
            <Button fx:id="toggleThemeBtn" text="🌙 Dark Mode" style="-fx-background-radius: 10; -fx-background-color: #444; -fx-text-fill: white;" />
        </HBox>
    </top>

    <!-- Center Content -->
    <center>
        <VBox spacing="10" padding="20">
            <HBox spacing="10">
                <TextField fx:id="searchField" promptText="Search tasks..." />
                <ComboBox fx:id="priorityFilter" promptText="Filter by priority" />
                <ProgressBar fx:id="completionProgress" prefWidth="200"/>
            </HBox>

            <ListView fx:id="taskListView" prefHeight="400"/>
        </VBox>
    </center>

    <!-- Footer -->
    <bottom>
        <HBox spacing="10" padding="10" alignment="CENTER_RIGHT">
            <Button fx:id="addTaskBtn" text="➕ Add Task" style="-fx-background-color: #3b82f6; -fx-text-fill: white;" />
        </HBox>
    </bottom>

</BorderPane>
